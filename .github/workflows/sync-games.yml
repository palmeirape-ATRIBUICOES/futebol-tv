name: '⚽ Auto Sync Games'

on:
  schedule:
    # Runs every 30 minutes, 24/7
    - cron: '*/30 * * * *'
  workflow_dispatch:
    # Manual trigger from GitHub Actions UI

permissions:
  contents: read

jobs:
  sync:
    name: 'Sync Games from Source'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: '📥 Checkout'
        uses: actions/checkout@v4

      - name: '⚙️ Setup Node.js'
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: '📦 Install Dependencies'
        run: npm install

      - name: '🔄 Run Sync'
        env:
          FIREBASE_ADMIN_EMAIL: ${{ secrets.FIREBASE_ADMIN_EMAIL }}
          FIREBASE_ADMIN_PASSWORD: ${{ secrets.FIREBASE_ADMIN_PASSWORD }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node scripts/sync-games.js
